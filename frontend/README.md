# Frontend

React + Tailwind application for SkillVault.

## Setup

```bash
cd frontend
npm install
npm run dev
```

The app currently shows a placeholder home page. Add pages, components, and API service files under `src/`.
